services:
  whatsapp-agent:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    # Logs persistidos pelo daemon do Docker; estado da sessão fica no Supabase.
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'
